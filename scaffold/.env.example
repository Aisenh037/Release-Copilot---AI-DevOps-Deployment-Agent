# Copy to scaffold/.env and fill in real values. Names verified against `swytchcode info
# <canonical_id>` output and live/dry-run `swytchcode exec` calls on 2026-08-09 (Task 2
# Golden Path spike) — see docs/superpowers/plans/swytchcode-methods.md for the full
# per-method contract table and verification evidence. No renames were needed: all
# pre-existing names below resolved correctly.

# Groq (used directly by the Next.js app via the Vercel AI SDK, not via Swytchcode)
GROQ_API_KEY=
GROQ_MODEL=
GROQ_FALLBACK_MODEL=

# Swytchcode credential env vars (verified: env var takes priority over
# ~/.swytchcode/credentials and .env; passed straight through to `exec`/runtime calls,
# no manual header construction needed)
GITHUB_API_KEY=
JIRA_API_KEY=
JIRA_EMAIL=
JIRA_SITE=
NETLIFY_API_KEY=
NOTION_API_KEY=

# Demo constants
RELEASE_REPO=
RELEASE_BRANCH=
JIRA_PROJECT_KEY=
NETLIFY_SITE_ID=
NOTION_PARENT_PAGE_ID=
NEXT_PUBLIC_JIRA_SITE=
